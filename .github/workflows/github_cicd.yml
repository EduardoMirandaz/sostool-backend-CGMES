name: CI

on:
  push:
    branches: ["ops"]
  pull_request:
    branches: ["ops"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build_job:
    runs-on: ubuntu-latest
    container: maven:3.6.3-openjdk-11
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Build
        run: |
          mvn clean install -DskipTests -f ./pom.xml

  unit_test_job:
    needs: build_job
    runs-on: ubuntu-latest
    container: maven:3.6.3-openjdk-11
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Unit tests
        run: |
          mvn test -f ./pom.xml

  build:
    name: Build Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Build Docker image
        run: docker build -t EduardoMirandaz/sostool-backend-CGMES:latest .

      - name: Log into GitHub Container Registry
        run: echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Push Docker image
        run: docker push ghcr.io/EduardoMirandaz/sostool-backend-CGMES:latest